---
title: "Install the Cool Computers CLI"
sidebarTitle: "Install the CLI"
description: "Install the official native Cool Computers CLI on macOS, Linux, or Windows."
---

Install the native `cool` command directly. It does not require Node.js or Python.

## macOS and Linux

```sh
curl -fsSL https://cool.computer/install.sh | sh
```

## Windows PowerShell

```powershell
(iwr -UseBasicParsing https://cool.computer/install.ps1).Content | iex
```

The installer adds `cool` to the current PowerShell session and your user `PATH`. Restart terminal apps that were already open before installation.

## Update and remove

Run the installer for your platform again to update. Follow the [CLI maintenance instructions](https://www.cool.computer/install#update-and-remove) to remove the executable and its platform-specific environment settings.

Continue with [the CLI guide](/use/cli) to sign in and operate your computer.
